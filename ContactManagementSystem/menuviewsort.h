#pragma once
// removes warning

#ifndef MENUVIEWSORT_H
#define MENUVIEWSORT_H


void selectionSort(struct contact *sourceHead,struct contact *sourceTail, struct contact *tmpHeadContact, struct contact *tmpTailContact, char type);

#endif